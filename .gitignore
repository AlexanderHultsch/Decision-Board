# Local configuration — never commit
config/config.local.json
config/*.local.json
config/*.bak

# The board's memory (decision 0005): program content, must never be
# committed
memory/

# The default audit folder the setup wizard proposes
audit/

# Ask the vault threads (spec 10): conversations with the vault, never committed
config/threads/
threads/

# Secrets — never commit
.env
.env.*
*.pem
*.key
*.pfx
*.cer
*.crt

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# OS and editor
Thumbs.db
desktop.ini
.DS_Store
.vscode/
.idea/
